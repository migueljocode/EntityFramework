<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
        <Style TargetType="{x:Type RadioButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="Border"
                                    Background="Transparent"
                                    BorderBrush="Gray"
                                    BorderThickness="1"
                                    CornerRadius="10"
                                    Width="20"
                                    Height="20"
                                    Margin="5">
                                <Ellipse Margin="1.5" x:Name="Ellipse" 
                                         Fill="Transparent" 
                                         Stroke="Gray" 
                                         StrokeThickness="0"/>
                            </Border>
                        <ContentPresenter x:Name="Content"
                                          Margin="5,0,0,0" 
                                          VerticalAlignment="Center">
                        </ContentPresenter>
                    </StackPanel>
                        <ControlTemplate.Triggers>
                            
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Ellipse" Property="Fill" Value="{StaticResource BlueLinearGradient}"/>
                                <!--<Setter Property="Foreground" Value="{StaticResource BlueLinearGradient}"/>-->
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard Storyboard.TargetProperty="(Foreground).(SolidColorBrush.Color)">
                                        <ColorAnimation Duration="0:0:0.25"
                                                        From="Gray"
                                                        To="#0083B0"/>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         Storyboard.TargetName="Ellipse"
                                                         Duration="0:0:0.25"
                                                         From="0"
                                                         To="1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard Storyboard.TargetProperty="(Foreground).(SolidColorBrush.Color)">
                                        <ColorAnimation Duration="0:0:0.25"
                                                        From="#0083B0"
                                                        To="Gray"/>
                                        <DoubleAnimation Storyboard.TargetName="Ellipse"
                                                         Storyboard.TargetProperty="Opacity"
                                                         Duration="0:0:0.25"
                                                         From="1"
                                                         To="0"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                            
                            <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Border" Property="BorderBrush" Value="DarkBlue"/>
                            </Trigger>
                            
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="FontSize" Value="16"/>
        </Style>
</ResourceDictionary>